name: app1
version: 1.0
container: singularity_image_app1.sif
description: "Performs preprocessing on BIDS datasets."
default_params:
  flag1: true
  flag2: 0.5
required_inputs:
  - bids_dir
  - output_dir
optional_inputs:
  - config_file

