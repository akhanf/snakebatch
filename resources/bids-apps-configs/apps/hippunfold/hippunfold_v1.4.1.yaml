name: hippunfold
version: v1.4.1
container: '/srv/containers/hippunfold_v1.4.1.sif'
analysis_level: participant
default_opts: '--modality T1w --atlas bigbrain -c all --until import_t1'
input_to_filter: T1w  #must use the input name from the app
resources:
  cores: 8
  mem_mb: 32000
  runtime: 360 
  gpus: 0
args: "{input} {output} participant"

